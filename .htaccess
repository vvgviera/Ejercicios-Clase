# --- Establecer archivo de inicio ---
DirectoryIndex index.html

# --- Evitar listar directorios ---
Options -Indexes

# --- Proteger nota.txt para que NO sea accesible desde el navegador ---
<Files "nota.txt">
    Order allow,deny
    Deny from all
</Files>

# --- Bloquear acceso a archivos sensibles ---
<FilesMatch "\.(htaccess|htpasswd|ini|log|env)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# --- Evitar ejecución de scripts en carpetas no deseadas ---
AddHandler cgi-script .php .php5 .php7 .phtml
Options -ExecCGI

# --- Seguridad básica adicional ---
# Evitar que se muestren errores al usuario
php_flag display_errors Off

# Forzar UTF‑8
AddDefaultCharset UTF-8
